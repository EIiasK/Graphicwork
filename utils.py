import numpy as np

def print_matrix(matrix, label="Matrix"):
    print(f"{label}:")
    for row in range(4):
        print([matrix[row][col] for col in range(4)])


def load_texture(path):
    # 纹理加载函数
    pass

